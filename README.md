# Django CRM

Simple CRM system built with Django.

## Installation

```bash
git clone https://github.com/z1kmus/Django-CRM.git
cd django-crm
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
